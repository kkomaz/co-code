<%= form_for [@language, @problem, @room], :url => problem_rooms_path(@language, @problem) do |f| %>
   <%= f.label :title %>
   <%= f.text_field :title %>
   <%= f.submit "Start a chat!" %>
<% end %>

