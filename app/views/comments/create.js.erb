var newDiv = '<%= j(render "comments/comment", :comment => @comment) %>'
var $container = $("div#comment-list")
$("textarea").val("");
$("textarea").focus();
$container.append(newDiv);

$("body").animate({
    scrollTop: $("div[data-id=" + "<%= @comment.id %>" + "] > div").offset().top
});