<div class="container">
  <div class="row">
    <div class="col-md-4 col-md-offset-8 col-xs-3 col-xs-offset-7">
      <div class="dropdown">
          <div class="pull-right">
            <button class="btn btn-primary dropdown-toggle button-padding" type="button" id="dropdownMenu1" data-toggle="dropdown" aria-expanded="true">
              finished users: <%= @users_finished.count %>
                <span class="caret"></span>
            </button>
            <ul class="dropdown-menu user-list" role="menu" aria-labelledby="dropdownMenu1">
              <%= render partial: 'problems/user_dropdown', collection: @users_finished, as: :user %>
            </ul>
          </div>
      </div>
      <div class="dropdown">
        <div class="pull-right">
          <button class="btn btn-primary dropdown-toggle" type="button" id="dropdownMenu1" data-toggle="dropdown" aria-expanded="true">
            working users: <%= @users_working.count %>
              <span class="caret"></span>
          </button>
          <ul class="dropdown-menu user-list" role="menu" aria-labelledby="dropdownMenu1">
            <%= render partial: 'problems/user_dropdown', collection: @users_working, as: :user %>
          </ul>
        </div>
      </div>
    </div>
  </div>
  <div class="row top-pad">
    <div class="col-md-6 col-sm-12">
      <div class="panel bread-crumb">
        <h6>
          <%= link_to progress_path(@language.slug) do %>
            <i class="icon-<%= language_icon(@language) %>"></i> <%= @language.name %>
          <%- end -%> / <%= problem_number(@problem) %>
        </h6>
      </div>
    </div>
    <div class="col-md-3 col-sm-12">
      <div class="panel problem-buttons">
          <%= progress_buttons(@language_problem) %>
      </div>
    </div>
    <div class="col-md-3 col-sm-12">
      <div class="panel problem-buttons">
          <%= favorite_buttons(@language_problem) %>
      </div>
    </div>
  </div>

  <div class="row">
    <div class="col-md-12">
      <div class="panel problem-body">
        <div class="problem-title">
          <h2><%= problem_title(@problem) %></h2>
        </div>
        <div class="problem-content">
          <%= raw @problem.content %>
        </div>
      </div>
    </div>
  </div>
<div class="box-footer">
    <div class="col-md-6 chat-padding">
      <div class="panel">
        <div class="top-fix">
          <%= link_to 'Create a chatroom!', new_problem_room_path(@language.slug, @problem.slug), :class => 'btn btn-primary'  %> <br> <br>
        </div>
        <% if @rooms %>
          <% @rooms.each.with_index(1) do |room,index| %> 
            <%= link_to "Room #{index}.", problem_room_path(@language.slug, @problem.slug, room), :class => 'btn btn-block btn-inverse' %>
          <% end %>
        <% end %>
      </div>
      <div class="post-box">
        <%= render @posts %>
      </div>
      <div id="paginator" id="center-paginate">
        <%= paginate @posts,:remote => true %>
      </div>
        <%#= link_to "Show all posts!", problem_posts_path(@language.slug, @problem.slug) %>
    </div>
    <div class="col-md-6 post-padding">
      <div class="panel">
        <h6 style="text-align: center;"><strong>Stuck?</strong> Just ask a question!</h6>
      </div>
      <%= render :partial => 'posts/form' %>
    </div>
</div>

</div>

